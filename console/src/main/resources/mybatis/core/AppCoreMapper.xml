<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.somta.juggle.console.infrastructure.mapper.AppMapper" >
<!-- Result Map-->
<resultMap id="BaseResultMap" type="net.somta.juggle.console.infrastructure.po.AppPO" >
	<result column="id" property="id"/>
	<result column="app_name" property="appName"/>
	<result column="app_code" property="appCode"/>
	<result column="remark" property="remark"/>
	<result column="deleted" property="deleted"/>
	<result column="created_at" property="createdAt"/>
	<result column="created_by" property="createdBy"/>
	<result column="updated_at" property="updatedAt"/>
	<result column="updated_by" property="updatedBy"/>
</resultMap>

<sql id="Example_Where_Clause">
	where 1=1 and deleted=0
	<if test="appName != null and appName != ''">
		and app_name like concat("%", #{appName}, "%")
	</if>
	<if test="appCode != null and appCode != ''">
		and app_code = #{appCode}
	</if>
</sql>
       
<!-- t_object table all fields -->
<sql id="Base_Column_List" >
	id, app_code, app_name, remark, created_at, created_by, updated_at, updated_by, deleted
</sql>

<insert id="add" parameterType="net.somta.juggle.console.infrastructure.po.AppPO" >
	insert into t_app (id, app_code, app_name, remark, deleted, created_at, created_by, updated_at, updated_by)
	values (#{id},#{appCode},#{appName},#{remark},0,now(),#{createdBy},now(),#{updatedBy});
</insert>

<delete id="deleteById" parameterType="java.lang.Long">
 delete from t_app where id=#{id}
</delete>

<update id="update" parameterType="net.somta.juggle.console.infrastructure.po.AppPO" >
	update t_app set
	<trim  suffixOverrides="," >
		<if test="appName != null and appName != ''">
			app_name=#{appName},
		</if>
		<if test="appCode != null and appCode != ''">
			app_code=#{appCode},
		</if>
		<if test="remark != null and remark != ''">
			remark=#{remark},
		</if>
		<if test="deleted != null">
			deleted=#{deleted},
		</if>
		<if test="createdAt != null">
			created_at=#{createdAt},
		</if>
		<if test="createdBy != null">
			created_by=#{createdBy},
		</if>
		updated_at=now(),
		<if test="updatedBy != null">
			updated_by=#{updatedBy},
		</if>
	</trim> where id=#{id}
</update>

<select id="queryById" resultMap="BaseResultMap" parameterType="java.lang.Long">
	select <include refid="Base_Column_List" />
	from t_app where id = #{id}
</select>

<select id="queryListCount" resultType="java.lang.Long" parameterType="net.somta.juggle.console.interfaces.param.app.AppQueryParam">
	select count(*)
	from t_app
	<include refid="Example_Where_Clause"/>
</select>

<select id="queryByList" resultMap="BaseResultMap" parameterType="net.somta.juggle.console.interfaces.param.app.AppQueryParam">
	select <include refid="Base_Column_List" />
	from t_app
	<include refid="Example_Where_Clause"/>
</select>

</mapper>   
